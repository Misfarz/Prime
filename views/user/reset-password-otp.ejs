<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Verify OTP - Prime</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="h-screen w-screen flex bg-black text-white">

  <div class="hidden md:block w-1/2 h-full bg-cover bg-center" style="background-image: url('../../');">
    <div class="h-full w-full bg-black bg-opacity-40 flex items-start p-10">
      <h1 class="text-white text-4xl font-bold">Prime.</h1>
    </div>
  </div>

  <div class="w-full md:w-1/2 h-full flex items-center justify-center p-6">
    <form class="w-full max-w-md space-y-6" action="/verify-reset-otp" method="post">
      <div>
        <h2 class="text-xl font-semibold mb-2">Verify OTP</h2>
        <% if (error && error.length > 0) { %>
          <div class="text-red-700 px-4 py-3 relative mt-4">
            <%= error %>
          </div>
        <% } %>
        <p class="text-sm text-gray-300">Enter the OTP sent to your email address</p>
      </div>

      <input type="hidden" name="email" value="<%= email %>" />

      <div class="space-y-1">
        <label class="text-sm font-medium">OTP <span class="text-red-500">*</span></label>
        <input type="text" placeholder="Enter OTP" name="otp" class="w-full p-3 rounded bg-white text-black focus:outline-none" required />
      </div>

      <button type="submit" class="w-full bg-white text-black font-semibold py-3 rounded hover:bg-gray-200">VERIFY OTP</button>

      <div class="flex justify-center mt-4">
        <p class="text-sm text-gray-300">Didn't receive the OTP?</p>
        <form action="/resend-reset-otp" method="post" class="ml-2">
          <input type="hidden" name="email" value="<%= email %>" />
          <button type="submit" class="text-sm underline text-white hover:text-gray-200">Resend OTP</button>
        </form>
      </div>

      <div class="text-center text-sm text-gray-300 mt-4">
        <a href="/login" class="underline text-white hover:text-gray-200">Back to Login</a>
      </div>
    </form>
  </div>
</body>
</html>
