<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login - Prime</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Inter', sans-serif;
    }
    .auth-divider {
      display: flex;
      align-items: center;
      color: #6b7280;
      font-size: 0.75rem;
    }
    .auth-divider::before,
    .auth-divider::after {
      content: "";
      flex: 1;
      border-bottom: 1px solid #374151;
    }
    .auth-divider::before {
      margin-right: 0.5rem;
    }
    .auth-divider::after {
      margin-left: 0.5rem;
    }
    .auth-container {
      background-color: rgba(0, 0, 0, 0.7);
      backdrop-filter: blur(4px);
    }
  </style>
</head>
<body class="min-h-screen flex items-center justify-center p-4 bg-yellow text-white" style="background-image: url('https://images.unsplash.com/photo-1618005182384-a83a8bd57fbe?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1064&q=80'); background-size: cover; background-position: center;">
  <div class="w-full max-w-md p-8 rounded-lg auth-container">
    <!-- Logo -->
    <div class="mb-8 text-center">
      <h1 class="text-3xl font-bold">Prime.</h1>
    </div>

    <!-- Error Messages -->
    <% if (error_msg && error_msg.length > 0) { %>
      <div class="mb-4 p-3 text-sm text-red-400 bg-red-900 bg-opacity-50 rounded">
        <%= error_msg %>
      </div>
    <% } %>
    <% if (message) { %>
      <div class="mb-4 p-3 text-sm text-blue-400 bg-blue-900 bg-opacity-50 rounded">
        <%= message %>
      </div>
    <% } %>

    <!-- Login Form -->
    <form class="space-y-4" action="/login" method="post">
      <div>
        <input 
          type="email" 
          placeholder="Email address" 
          name="email" 
          class="w-full px-4 py-3 text-sm bg-gray-900 bg-opacity-70 border border-gray-700 rounded focus:outline-none focus:ring-1 focus:ring-gray-500" 
          required
          autocomplete="email"
        >
      </div>

      <div>
        <input 
          type="password" 
          placeholder="Password" 
          name="password" 
          class="w-full px-4 py-3 text-sm bg-gray-900 bg-opacity-70 border border-gray-700 rounded focus:outline-none focus:ring-1 focus:ring-gray-500" 
          required
          autocomplete="current-password"
        >
      </div>

      <div class="flex items-center justify-between text-sm">
        <label class="flex items-center text-gray-400">
          <input type="checkbox" class="mr-2 h-4 w-4 rounded bg-gray-700 border-gray-600">
          Remember me
        </label>
        <a href="/forgot-password" class="text-blue-500 hover:text-blue-400">Forgot password?</a>
      </div>

      <button 
        type="submit" 
        class="w-full py-3 text-sm font-medium text-white bg-blue-600 rounded hover:bg-blue-700 transition-colors"
      >
        Sign In
      </button>
    </form>

    <!-- Divider -->
    <div class="my-6 auth-divider">
      <span>or continue with</span>
    </div>

    <!-- Social Login -->
    <div class="space-y-3">
      <a 
        href="/auth/google/login" 
        class="flex items-center justify-center gap-2 w-full py-2.5 px-4 text-sm border border-gray-700 rounded hover:bg-gray-900 hover:bg-opacity-50 transition-colors"
      >
        <img src="https://www.google.com/favicon.ico" class="w-4 h-4" alt="Google">
        Google
      </a>
    </div>

    <!-- Footer -->
    <div class="mt-8 text-center text-sm text-gray-400">
      <p>Don't have an account? <a href="/signup" class="text-blue-500 hover:text-blue-400">Sign up</a></p>
    </div>
  </div>
</body>
</html>