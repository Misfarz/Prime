<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Login - Prime</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="h-screen w-screen flex bg-black text-white">

  <div class="hidden md:block w-1/2 h-full bg-cover bg-center" style="background-image: url('../../');">
    <div class="h-full w-full bg-black bg-opacity-40 flex items-start p-10">
      <h1 class="text-white text-4xl font-bold">Prime.</h1>
    </div>
  </div>

  
  <div class="w-full md:w-1/2 h-full flex items-center justify-center p-6">
    <form class="w-full max-w-md space-y-6" action="/login" method="post">
      <div>
        <h2 class="text-xl font-semibold mb-2">Login / Join Us</h2>
        <% if (error_msg && error_msg.length> 0) { %>
          <div class="text-red-700 px-4 py-3 relative mt-4">
            <%= error_msg %>
          </div>
          <% } %>
        <p class="text-sm text-gray-300">Enter your email and password to log in</p>
      </div>
      <% if (message) { %>
        <p class="text-red-500 text-xs mt-1"><%= message %></p>
      <% } %>

      <div class="space-y-1">
        <label class="text-sm font-medium" >EMAIL <span class="text-red-500">*</span></label>
        <input type="email" placeholder="Email" name="email" class="w-full p-3 rounded bg-white text-black focus:outline-none" required />
      </div>

      <div class="space-y-1">
        <label class="text-sm font-medium">PASSWORD <span class="text-red-500">*</span></label>
        <input type="password" placeholder="Password" name="password" class="w-full p-3 rounded bg-white text-black focus:outline-none" required />
      </div>

      <div class="flex items-center">
        <input type="checkbox" class="mr-2" id="remember" />
        <label for="remember" class="text-sm">Remember me on this computer</label>
      </div>

      <button type="submit" class="w-full bg-white text-black font-semibold py-3 rounded hover:bg-gray-200">LOGIN</button>

      <div class="text-xs text-gray-400">
        By logging in, I confirm that I have read and accept the
        <a href="#" class="underline">Terms and Conditions</a> and the
        <a href="#" class="underline">Privacy Policy</a>.
      </div>

      <div class="flex justify-between text-sm">
        <a href="#" class="underline">LOGIN WITH OTP</a>
        <a href="#" class="underline">FORGOTTEN YOUR PASSWORD?</a>
      </div>

      <div class="mt-4">
        <a href="/auth/google/login" type="button" class="w-full border border-white py-2 rounded hover:bg-white hover:text-black flex items-center justify-center gap-2">
          <img src="https://www.google.com/favicon.ico" class="w-5 h-5" alt="Google" />
          Login with Google
        </a>
      </div>


    
      <div class="text-center text-sm text-gray-300 mt-4">
        Don’t have an account?
        <a href="/signup" class="underline text-white hover:text-gray-200">Sign Up</a>
      </div>
    </form>
  </div>
</body>
</html>


