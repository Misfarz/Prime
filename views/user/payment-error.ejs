<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment Failed - Prime</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body class="bg-gray-50">
    <%- include('../partials/user/header') %>

    <div class="container mx-auto px-4 py-12">
        <div class="max-w-lg mx-auto bg-white rounded-lg shadow-md overflow-hidden">
            <div class="p-8 text-center">
                <div class="mb-6">
                    <div class="inline-flex items-center justify-center w-16 h-16 rounded-full bg-red-100 text-red-500 mb-4">
                        <i class="fas fa-exclamation-circle text-3xl"></i>
                    </div>
                    <h1 class="text-2xl font-bold text-gray-800 mb-2">Payment Failed</h1>
                    <p class="text-gray-600 mb-6">
                        <%= errorMessage || "We couldn't process your payment. Please try again or choose a different payment method." %>
                    </p>
                    
                    <% if (orderId) { %>
                        <div class="text-sm text-gray-500 mb-6">
                            <p>Order Reference: <%= orderId %></p>
                            <% if (errorCode) { %>
                                <p>Error Code: <%= errorCode %></p>
                            <% } %>
                        </div>
                    <% } %>
                </div>
                
                <div class="flex flex-col space-y-3 sm:flex-row sm:space-y-0 sm:space-x-4 justify-center">
                    <a href="/checkout" class="inline-flex items-center justify-center px-6 py-3 border border-transparent text-base font-medium rounded-md shadow-sm text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500">
                        <i class="fas fa-redo mr-2"></i> Retry Payment
                    </a>
                    <a href="/" class="inline-flex items-center justify-center px-6 py-3 border border-gray-300 shadow-sm text-base font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500">
                        <i class="fas fa-home mr-2"></i> Return Home
                    </a>
                </div>
                
                <div class="mt-8 text-sm text-gray-500">
                    <p>If you continue to experience issues, please contact our support team.</p>
                    <p class="mt-2">
                        <a href="/contact" class="text-blue-600 hover:text-blue-800 hover:underline">
                            <i class="fas fa-headset mr-1"></i> Contact Support
                        </a>
                    </p>
                </div>
            </div>
            
            <div class="bg-gray-50 px-8 py-4 border-t border-gray-200">
                <div class="text-center">
                    <h3 class="text-sm font-medium text-gray-500">Other Payment Options</h3>
                    <div class="mt-3 flex justify-center space-x-6">
                        <div class="flex items-center">
                            <i class="fas fa-money-bill-wave text-gray-400 mr-2"></i>
                            <span class="text-sm text-gray-600">Cash on Delivery</span>
                        </div>
                        <div class="flex items-center">
                            <i class="fas fa-wallet text-gray-400 mr-2"></i>
                            <span class="text-sm text-gray-600">Wallet</span>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <%- include('../partials/user/footer') %>
</body>
</html>
