<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Forgot Password - Prime</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="h-screen w-screen flex bg-black text-white">

  <div class="hidden md:block w-1/2 h-full bg-cover bg-center" style="background-image: url('https://images.unsplash.com/photo-1618005182384-a83a8bd57fbe?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1064&q=80');">
    <div class="h-full w-full bg-black bg-opacity-40 flex items-start p-10">
      <h1 class="text-white text-4xl font-bold">Prime.</h1>
    </div>
  </div>

  <div class="w-full md:w-1/2 h-full flex items-center justify-center p-6">
    <form class="w-full max-w-md space-y-6" action="/forgot-password" method="post">
      <div>
        <h2 class="text-3xl font-bold mb-2">Reset Password</h2>
        <% if (error && error.length > 0) { %>
          <div class="text-red-500 px-4 py-3 rounded bg-red-900 bg-opacity-30 mb-4">
            <%= error %>
          </div>
        <% } %>
        <% if (success && success.length > 0) { %>
          <div class="text-green-500 px-4 py-3 rounded bg-green-900 bg-opacity-30 mb-4">
            <%= success %>
          </div>
        <% } %>
        <p class="text-sm text-gray-400">Enter your email to receive a password reset link</p>
      </div>

      <div class="space-y-2">
        <label class="text-sm font-medium text-gray-400">Email Address <span class="text-red-500">*</span></label>
        <input 
          type="email" 
          placeholder="your@email.com" 
          name="email" 
          class="w-full p-3 rounded bg-gray-900 border border-gray-700 focus:outline-none focus:ring-1 focus:ring-gray-500" 
          required
          autocomplete="email"
        />
      </div>

      <button type="submit" class="w-full bg-blue-600 text-white font-medium py-3 rounded hover:bg-blue-700 transition-colors">
        Send Reset Link
      </button>

      <div class="text-center text-sm text-gray-400 mt-4">
        Remember your password? <a href="/login" class="text-blue-500 hover:text-blue-400">Sign in here</a>
      </div>
    </form>
  </div>

  <script>
    document.addEventListener('DOMContentLoaded', function() {
      // Auto-focus email field for better UX
      const emailInput = document.querySelector('input[name="email"]');
      if (emailInput) {
        emailInput.focus();
      }
    });
  </script>
</body>
</html>